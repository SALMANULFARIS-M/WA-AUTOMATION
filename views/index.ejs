<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WhatsApp Bulk Sender</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-br from-green-50 via-blue-50 to-indigo-100 min-h-screen font-sans"
  >
    <!-- Main Container -->
    <div class="max-h-screen overflow-hidden md:overflow-visible">
      <!-- Mobile: Scrollable, Desktop: Fit to screen -->
    <div class="h-screen md:h-auto overflow-y-auto md:overflow-visible">
        <div class="max-w-7xl mx-auto p-3 md:p-6">
         <!-- Header -->
           <div class="text-center mb-4 md:mb-6">
            <div
              class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-green-500 to-blue-500 rounded-2xl mb-3 shadow-lg"
            >
              <span class="text-2xl">📤</span>
            </div>
            <h1
              class="text-2xl md:text-3xl font-bold bg-gradient-to-r from-green-600 to-blue-600 bg-clip-text text-transparent"
            >
              WhatsApp Bulk Messaging Dashboard
            </h1>
          </div>

          <!-- Messages -->
          <div class="mb-4">
            <% if (messages.success) { %>
            <div
              class="bg-green-50 border-l-4 border-green-400 text-green-800 px-4 py-2 rounded-r-lg mb-2 shadow-sm"
            >
              <%= messages.success %>
            </div>
            <% } %> <% if (messages.error) { %>
            <div
              class="bg-red-50 border-l-4 border-red-400 text-red-800 px-4 py-2 rounded-r-lg mb-2 shadow-sm"
            >
              <%= messages.error %>
            </div>
            <% } %>
          </div>

          <!-- Main Grid Layout -->
          <div class="grid grid-cols-1 lg:grid-cols-3 gap-4 md:gap-6">
            <!-- Left Column: Upload Form -->
            <div class="lg:col-span-2">
              <div
                class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-4 md:p-6"
              >
                <h2
                  class="text-xl font-semibold text-gray-800 mb-4 flex items-center"
                >
                  <span
                    class="w-6 h-6 bg-blue-500 rounded-full mr-2 flex items-center justify-center text-white text-sm"
                    >1</span
                  >
                  Setup Campaign
                </h2>

                <form
                  action="/upload"
                  method="POST"
                  enctype="multipart/form-data"
                  class="space-y-4"
                >
                  <!-- File Uploads Row -->
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                      <label
                        class="block text-sm font-medium text-gray-700 mb-2"
                        >📊 Excel File</label
                      >
                      <div class="relative">
                        <input
                          type="file"
                          name="excel"
                          accept=".xlsx"
                          required
                          class="w-full border-2 border-dashed border-gray-300 rounded-xl p-3 text-sm hover:border-blue-400 focus:border-blue-500 focus:outline-none transition-colors"
                        />
                      </div>
                    </div>

                    <div>
                      <label
                        class="block text-sm font-medium text-gray-700 mb-2"
                        >🖼️ Image</label
                      >
                      <div class="relative">
                        <input
                          type="file"
                          name="image"
                          accept="image/*"
                          required
                          class="w-full border-2 border-dashed border-gray-300 rounded-xl p-3 text-sm hover:border-blue-400 focus:border-blue-500 focus:outline-none transition-colors"
                        />
                      </div>
                    </div>
                  </div>

                  <!-- Message Textarea -->
                  <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2"
                      >✉️ Message Template</label
                    >
                    <textarea
                      name="message"
                      rows="4"
                      required
                      class="w-full border-2 border-gray-200 rounded-xl p-3 text-sm focus:border-blue-500 focus:outline-none resize-none transition-colors"
                      placeholder="Enter your message template here..."
                    >
നിക്ഷേപം നടത്തി നഷ്ടപ്പെട്ടു കഴിഞ്ഞോ? ഇനി അതിന് ഒരു full stop വെക്കാം!

നിങ്ങൾക്കായി എത്തി iQue Cap – ഇന്നത്തെ ഏറ്റവും വിശ്വസനീയമായ, വിജയം ഉറപ്പുള്ള ഇൻവെസ്റ്റ്‌മെന്റ് പ്ലാറ്റ്ഫോം!

📈 ഇനി റിസ്ക് അല്ല, ഗ്യാരണ്ടിയുള്ള Return on Investment
💼 വ്യത്യസ്ത ആവശ്യങ്ങൾക്കനുസരിച്ച് വിവിധ ഇൻവെസ്റ്റ്‌മെന്റ് പാക്കേജുകൾ
📊 ചെറിയ തുകയോടെ ആരംഭിക്കാം – വലിയ ലാഭം നേടാം
🔓 പുതിയ സ്ലോട്ടുകൾ ഇപ്പോൾ തുറന്നിട്ടുണ്ട്!

നിങ്ങൾ എത്രയും വേഗം തീരുമാനമെടുക്കുന്നത് മാത്രമാണ് ബാക്കിയുള്ളത്.
ഇത്ര തുക നിക്ഷേപിച്ചാൽ, ഇത്ര ശതമാനം ലാഭം – അതും നേരിട്ട് നിങ്ങളുടെ അക്കൗണ്ടിലേക്ക്!

കൂടുതൽ വിവരങ്ങൾക്ക്, ഉടൻ തന്നെ Cap Advisor നെ ബന്ധപ്പെടൂ.
നിക്ഷേപം നല്ലതാകണം എങ്കിൽ iQue Cap ആയിരിക്കണം നിങ്ങളുടെ ആദ്യ തെരഞ്ഞെടുപ്പ്!</textarea
                    >
                  </div>

                  <button
                    type="submit"
                    class="w-full bg-gradient-to-r from-green-500 to-blue-500 text-white font-semibold py-3 px-6 rounded-xl hover:from-green-600 hover:to-blue-600 transform hover:scale-[1.02] transition-all duration-200 shadow-lg"
                  >
                    🚀 Prepare Campaign
                  </button>
                </form>
              </div>
            </div>

            <!-- Right Column: Controls & Status -->
            <div class="space-y-4">
              <!-- Control Panel -->
              <div
                class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-4 md:p-6"
              >
                <h2
                  class="text-xl font-semibold text-gray-800 mb-4 flex items-center"
                >
                  <span
                    class="w-6 h-6 bg-green-500 rounded-full mr-2 flex items-center justify-center text-white text-sm"
                    >2</span
                  >
                  Controls
                </h2>

                <div class="grid grid-cols-2 gap-2">
                  <form action="/start" method="POST">
                    <button
                      class="w-full bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-3 rounded-lg transition-colors text-sm"
                    >
                      ▶️ Start
                    </button>
                  </form>

                  <form action="/pause" method="POST">
                    <button
                      class="w-full bg-yellow-500 hover:bg-yellow-600 text-white font-medium py-2 px-3 rounded-lg transition-colors text-sm"
                    >
                      ⏸️ Pause
                    </button>
                  </form>

                  <form action="/resume" method="POST">
                    <button
                      class="w-full bg-cyan-500 hover:bg-cyan-600 text-white font-medium py-2 px-3 rounded-lg transition-colors text-sm"
                    >
                      ⏯️ Resume
                    </button>
                  </form>

                  <form action="/stop" method="POST">
                    <button
                      class="w-full bg-red-500 hover:bg-red-600 text-white font-medium py-2 px-3 rounded-lg transition-colors text-sm"
                    >
                      ⛔ Stop
                    </button>
                  </form>
                </div>
              </div>

              <!-- Status Panel -->
              <div
                class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-4 md:p-6"
              >
                <h2
                  class="text-xl font-semibold text-gray-800 mb-4 flex items-center"
                >
                  <span
                    class="w-6 h-6 bg-purple-500 rounded-full mr-2 flex items-center justify-center text-white text-sm"
                    >📊</span
                  >
                  Status
                </h2>

                <!-- Status Indicators -->
                <div class="space-y-3">
                  <!-- Status Badge -->
                  <div class="flex items-center justify-between">
                    <span class="text-sm text-gray-600">Status:</span>
                    <span
                      class="status-badge px-3 py-1 bg-green-100 text-green-800 rounded-full text-xs font-medium"
                    >
                      <%= status.status %>
                    </span>
                  </div>

                  <!-- Progress Bar -->
                  <div>
                    <div
                      class="flex justify-between text-sm text-gray-600 mb-1"
                    >
                      <span>Progress</span>
                      <span
                        ><%= status.progress.sent %> / <%= status.progress.total
                        %></span
                      >
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-2">
                      <% let progressPercent = 0; if (status.progress &&
                      status.progress.total) { progressPercent =
                      Math.round((status.progress.sent / status.progress.total)
                      * 100); } %>
                     <!-- Progress Bar -->
                     <div class="progress-bar bg-gradient-to-r from-green-500 to-blue-500 h-2 rounded-full transition-all duration-300" 
                      style="width: <%= progressPercent %>%"></div>
                    </div>
                  </div>

                  <!-- Status Details -->
                  <div class="grid grid-cols-2 gap-2 text-xs">
                    <div class="bg-gray-50 rounded-lg p-2">
                      <div class="text-gray-500">Running</div>
                      <div
                        class="font-semibold <%= status.isRunning ? 'text-green-600' : 'text-red-600' %>"
                      >
                        <%= status.isRunning ? 'Yes' : 'No' %>
                      </div>
                    </div>
                    <div class="bg-gray-50 rounded-lg p-2">
                      <div class="text-gray-500">Paused</div>
                      <div
                        class="font-semibold <%= status.shouldPause ? 'text-yellow-600' : 'text-green-600' %>"
                      >
                        <%= status.shouldPause ? 'Yes' : 'No' %>
                      </div>
                    </div>
                  </div>

                  <!-- Current Contact -->
                  <div class="border-t pt-3 mt-3">
                    <div class="text-xs text-gray-500 mb-1">
                      Current Contact
                    </div>
                   <!-- Current Contact -->
                    <div class="current-contact">
                        <% if (status.currentContact) { %>
                       <div class="bg-blue-50 rounded-lg p-2 text-sm">
                        <div class="font-medium text-blue-800">
                               <%= status.currentContact.number %>
                          </div>
                           <div class="text-blue-600 text-xs">
                           Contact <%= status.currentContact.index + 1 %> of <%= status.currentContact.totalContacts %>
                              </div>
                                   </div>
                             <% } else { %>
                              <div class="bg-gray-50 rounded-lg p-2 text-sm italic text-gray-500">
                                 No current contact
                                 </div>
                                   <% } %>
                         </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Footer -->
          <div class="text-center mt-4 md:mt-6">
            <p class="text-xs text-gray-500">
              💡 Upload your Excel file with contact numbers and customize your
              message template
            </p>
          </div>
        </div>
      </div>
    </div>
    <script>
      // Function to update the UI with new status
      function updateStatusUI(status) {
        // Update status badge
        document.querySelector(".status-badge").textContent = status.status;

        // Update progress
        const progressPercent =
          status.progress.total > 0
            ? Math.round((status.progress.sent / status.progress.total) * 100)
            : 0;
        document.querySelector(
          ".progress-bar"
        ).style.width = `${progressPercent}%`;
        document.querySelector(
          ".progress-count"
        ).textContent = `${status.progress.sent}/${status.progress.total}`;

        // Update running/paused indicators
        document.querySelector(".running-status").textContent = status.isRunning
          ? "Yes"
          : "No";
        document.querySelector(".paused-status").textContent =
          status.shouldPause ? "Yes" : "No";

        // Update current contact
        const contactElement = document.querySelector(".current-contact");
        if (status.currentContact) {
          contactElement.innerHTML = `
        <div class="font-medium text-blue-800">${
          status.currentContact.number
        }</div>
        <div class="text-blue-600 text-xs">
          Contact ${status.currentContact.index + 1} of ${
            status.currentContact.totalContacts
          }
        </div>
      `;
        } else {
          contactElement.innerHTML = `
        <div class="bg-gray-50 rounded-lg p-2 text-sm italic text-gray-500">
          No current contact
        </div>
      `;
        }
      }

      // Function to fetch status from server
      async function fetchStatus() {
        try {
          const response = await fetch("/api/status");
          const status = await response.json();
          updateStatusUI(status);
        } catch (error) {
          console.error("Error fetching status:", error);
        }
      }

      // Poll the server every 3 seconds
      setInterval(fetchStatus, 3000);

      // Initial load
      fetchStatus();
    </script>
  </body>
</html>
